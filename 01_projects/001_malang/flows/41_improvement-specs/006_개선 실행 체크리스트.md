---
유형: 개선-체크리스트
프로젝트: 001_malang
상태: 진행중
수정일: 2026-02-22
태그: [flow, improvement, checklist, realtime]
---

# 개선 실행 체크리스트

## 원칙
- 중복코드 제거보다 안정성(중복 실행 방지)을 먼저 적용한다.
- 적용 순서는 **Idempotency -> 맵 정리 -> 책임 분리 -> 중복 제거**를 유지한다.

## 1단계. Idempotency 적용 (완료/운영조정만 남음)
- [x] `sendFsmEvent`에 idempotencyKey 옵션 추가
- [x] `sendFsmEvent` 중복 키 TTL 캐시(`processedFsmEventKeys`) 적용
- [x] `applyFsmActions` 중복 실행 방지(`appliedActionKeys`) 적용
- [x] Realtime 이벤트 키 dedupe(`processedRealtimeEventKeys`) 적용
- [x] reconnect/disconnect 시 idempotency 캐시 초기화
- [ ] 서버 레벨 idempotencyKey 연동 방식 확정(API 스펙)
- [ ] idempotency skip 로그 집계 규칙 확정

## 2단계. 맵 정리 일원화 (완료)
- [x] `cleanupByResponseId`, `cleanupByItemId` 함수로 통합
- [x] `done/cancel/delete` 순서 역전 멱등 처리
- [x] stale mapping GC 규칙 적용

## 3단계. 이벤트 책임 분리 (완료)
- [x] `parseRealtimeEvent` 정규화 함수 분리
- [x] `routeRealtimeEvent` + `eventRouter` 테이블 전환
- [x] `response.done` 목적별 핸들러 분리

## 4단계. 중복 코드 제거 (완료)
- [x] 전사 가드 로직 공통화
- [x] action 처리 공통 유틸화
- [x] 로깅 포맷/키 표준화

## 검증 체크
- [x] 중복 이벤트 2회 수신 시 side effect 1회만 실행
- [ ] `done -> canceled` / `canceled -> done` 역순에서도 최종 상태 동일
- [x] 수동 침묵(stage 1/2), 종료 의도 이벤트에서 중복 전이 없음
- [ ] `payment=수표` 같은 비허용 슬롯 값 입력 시 슬롯 미채움 + 재질문 유지

## 진행 로그
- 2026-02-20: 1단계 idempotency 키/중복차단 적용(`sendFsmEvent`, `applyFsmActions`, realtime event dedupe)
- 2026-02-20: 2단계 시작. 맵 정리 함수(`registerResponsePurposeMapping`, `registerAssistantItemMapping`, `cleanupResponseMapping`, `cleanupAssistantItemMapping`) 도입 및 `response.done/canceled/deleted` 경로 공통화
- 2026-02-20: `flows/` 및 `flows/improvements/` 문서/캔버스 파일명을 숫자 prefix(`000_`, `001_`...)로 정규화하고 내부 링크 전부 갱신
- 2026-02-20: `problems/`, `logs/`, `decisions/`도 숫자 prefix 규칙으로 정규화(`000_문제 인덱스`, `000_로그 인덱스`, `000_결정사항 인덱스`)하고 프로젝트 메인 링크 갱신
- 2026-02-20: stale mapping GC 추가(`requestPurpose`, `response`, `active`, `assistant item` 맵에 TTL 기반 정리 + 주기 제한), 2단계 완료
- 2026-02-20: 3단계 시작. `parseRealtimeEvent` 정규화 함수 분리 후 `handleRealtimeMessage` 진입부에 적용
- 2026-02-20: 현재 적용 이유/작업 내용을 요약한 문서(`007_idempotency 적용 및 맵 정리 공통화`) 추가
- 2026-02-20: stale mapping GC 상세 문서(`008_stale mapping GC 상세 설계`) 추가
- 2026-02-20: `routeRealtimeEvent` + `eventRouter` 도입, 라우팅 대상(`session.updated`, `response.created`, `conversation.item.created`, `response.canceled/cancelled/failed`, `conversation.item.deleted`, `response.done`) 분리
- 2026-02-20: `response.done` 목적별 핸들러(`slot_detection`, `text_enrichment`, `fsm_reply`) 분리 및 `handleResponseDoneEvent`로 통합
- 2026-02-20: 구현 상세 문서 `009_이벤트 라우터 및 response.done 핸들러 분리` 추가
- 2026-02-20: 전사 가드 판단을 `decideTranscriptGuard`로 공통화하고 분기 블록 단순화, 구현 문서 `010_전사 가드 로직 공통화` 추가
- 2026-02-20: `applyFsmActions`에 actionHandlers 테이블 및 payload 유틸 도입, guard 로그 형식을 `[GUARD:<key>] key=value`로 표준화, 구현 문서 `011_action 처리 공통 유틸화 및 로그 키 표준화` 추가
- 2026-02-20: 수동 검증으로 중복 클릭(침묵/종료 연속 클릭) 시 중복 전이 방지 동작 확인, 검증 체크 2개 완료
- 2026-02-20: 즉시 재발화 중복 케이스 보강. `decideTranscriptGuard`에 `hasSpeechStarted=true` 경로 중복 판정 추가, 문서 `012_즉시 재발화 시 중복 전사 차단 보강` 추가
- 2026-02-20: 연속 발화 인식 회귀 조정. `hasSpeechStarted=true` 경로 중복 차단을 `pendingPhase !== none`으로 제한, 문서 `013_연속 발화 인식 회귀 조정` 추가
- 2026-02-20: runtime config에 `slotValuePolicyMap` 추가. 서버/어드민/UI/세션/FSM에 정책 반영하여 비허용 슬롯 값(예: payment=수표) 차단 경로 구현, 문서 `014_슬롯 값 정책 맵 추가 및 비허용 값 차단` 추가
- 2026-02-20: DB drift로 `migrate dev` 직접 적용이 중단되어 `db execute` + `migrate resolve --applied`로 `20260220102000_add_slot_value_policy_map` 적용 및 상태 동기화 완료(`migrate status` up-to-date 확인)
- 2026-02-20: runtime 저장 후 scenario runtime 캐시 무효화(`ScenarioRuntimeService.invalidate`) 연결, skip 오탐 키워드 축소, quantity/payment heuristic fallback 추가 및 테스트 보강, 문서 `015_runtime 캐시 무효화 및 수량 스킵 오탐 보강` 추가
- 2026-02-20: admin `roleplay-test-v2` UI 섹션을 컴포넌트 분리(`RecentSessionStatsSection`, `HintSection`, `PromptTraceSection`, `ManualEventSection`, `MorphAnalysisSection`, `EventLogSection`, `EndConfirmModal`), 문서 `016_시나리오 테스트 페이지 UI 컴포넌트 분리` 추가
- 2026-02-20: `roleplay-test-v2` 섹션 컴포넌트에 `RoleplayTestSectionStateContext`/`RoleplayTestSectionActionsContext` 도입으로 props drilling 완화
- 2026-02-20: 상단 제어 패널(`ControlSection`)까지 context 기반으로 분리하여 page 컴포넌트의 props 전달/렌더 블록 추가 축소
- 2026-02-20: `/roleplay-test`를 `/roleplay-test-v2` redirect로 전환하고 레거시 `RoleplayRealtimeTester` + `components/roleplay-realtime/**` 제거, `admin yarn build` 통과
- 2026-02-20: `handleRealtimeMessage`에서 이벤트별 상세 처리(`speech_started`, `transcription.completed`, `output_audio_transcript.*`, `output_audio_buffer.stopped`)를 개별 콜백으로 분리하고 핸들러 테이블 위임 구조로 정리
- 2026-02-20: `handleRealtimeMessage` 추가 축소 제안 문서 작성(파이프라인 4단계 추출, 핸들러 테이블 외부화, ref 래핑 서비스, 조기 종료 키 표준화)
- 2026-02-20: `handleRealtimeMessage` 공통 파이프라인 4단계 함수(`parseIncomingRealtimeEvent`, `shouldSkipDuplicateRealtimeEvent`, `appendRealtimeVerboseEventLog`, `dispatchRealtimeFallbackEvent`)로 재정리하고 fallback 핸들러 맵 `useMemo` 외부화
- 2026-02-20: `routeRealtimeEvent` 내부 `eventRouter`를 `routedRealtimeEventHandlers(useMemo)`로 외부화하고 엔트리 함수를 "조회/실행/처리여부 반환" 형태로 축소
- 2026-02-20: Realtime 이벤트 콜백 팩토리 모듈(`realtime-event-handlers.ts`) 분리(`createRoutedRealtimeEventHandlers`, `createFallbackRealtimeEventHandlers`) 및 엔트리 파일 흐름/역할 주석 유지
- 2026-02-20: `issueToken`의 긴 초기화 블록을 `resetRealtimeRefsBeforeIssueToken`/`resetUiStateBeforeIssueToken`으로 분리해 API 호출 본문 가독성 개선
- 2026-02-20: 공통 유틸(`realtime-event-utils.ts`)과 시나리오 stage 맵(`realtime-scenario.ts`)을 page에서 분리해 엔트리 파일 라인/밀도 추가 축소
- 2026-02-20: `useReducer` 전환 후 훅 의존성 경고를 정리하고 `resetUiStateBeforeIssueToken`/`resetCollectedData`를 `setManyFields` 기반으로 단순화, 문서 `025_roleplay-test-v2 useReducer 의존성 안정화 및 setMany 적용` 추가
- 2026-02-20: 상태 정의/리듀서/초기값/setter 팩토리를 `page-state.ts`로 분리하고 `RESET_FOR_ISSUE_TOKEN`/`RESET_COLLECTED_DATA` reducer action으로 리셋 경로를 단일 dispatch로 전환, `realtime-event-handlers.ts`를 `RoutedEventSpec` 기반 디스패처로 재구성(terminal response 이벤트 템플릿화), 문서 `026_page-state 모듈 분리 및 이벤트 스펙 디스패처 전환` 추가
- 2026-02-20: Realtime 연결 수명주기(`connectRealtime` 시도 본문, `disconnectRealtime`, issueToken 전 ref 초기화)를 `controllers/realtime-lifecycle-controller.ts`로 분리해 엔트리 오케스트레이션 밀도 축소, 문서 `027_realtime lifecycle controller 분리` 추가
- 2026-02-20: `roleplay-ops-controller`/`realtime-turn-controller`/`section-view-model`로 공격적 분리 1차 적용, `page.tsx` 라인 수 `3211 -> 2816` 축소 및 오케스트레이션 중심 구조로 재정렬, 문서 `028_roleplay-test-v2 공격적 컨트롤러 분리 1차` 추가
- 2026-02-20: `realtime-turn-utils` 중복 helper를 `page.tsx`에서 제거해 단일 소스로 수렴하고 dead module(`realtime-fsm-controller.ts`) 삭제, `page.tsx` 라인 수 `2578 -> 2152` 추가 축소, 문서 `029_roleplay-test-v2 공격적 컨트롤러 분리 2차` 추가
- 2026-02-20: `applyStatusSnapshot`/auto-silence effect를 `status-snapshot-controller`/`auto-silence-controller`로 분리해 엔트리 정책 로직을 축소, `page.tsx` 라인 수 `2152 -> 2082` 추가 축소, 문서 `030_roleplay-test-v2 공격적 컨트롤러 분리 3차` 추가
- 2026-02-22: `admin/components/voip-v2-test`에 대해 힌트 트리거 단일화(`transcript.done` 중심), 힌트 dedupe timestamp 안정화, 힌트 요청 메타 정보 UI 노출, visibility/backoff 폴링 최적화, 도메인별 hooks 분리, 테스트 파일 골격 추가를 적용하고 문서 `031_admin voip-v2-test 핸들러-슬라이스 개선 적용 요약 및 전략`을 추가
- 2026-02-22: `handler-slice-refactor` 스킬 내용을 캔버스로 시각화한 `032_handler-slice-refactor 스킬 맵.canvas`를 추가하고, `031_admin voip-v2-test 현재 로직.canvas` 및 전략 문서 연결 링크를 동기화
- 2026-02-22: Excalidraw 정리 요청에 따라 `033_handler-slice-refactor 스킬 맵.excalidraw.md`를 추가하고 인덱스/전략 문서 링크를 동기화

## 연결 노트
- [[01_projects/001_malang/flows/41_improvement-specs/003_idempotency 개선 방향|idempotency 개선 방향]]
- [[01_projects/001_malang/flows/41_improvement-specs/004_리얼타임 목적-응답-아이템 맵 정리 개선안|리얼타임 목적-응답-아이템 맵 정리 개선안]]
